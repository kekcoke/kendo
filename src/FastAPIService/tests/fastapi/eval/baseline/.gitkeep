# Baseline metric snapshots — generated by regenerate_baseline.py
#
# Each snapshot file records metric scores from a known-good run.
# The eval gate compares current scores against these baselines
# and fails if any metric regresses by more than 5%.
