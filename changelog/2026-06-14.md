# Day 12 — M3.1: Reverse Proxy Load Balancer
**MR:** [feat(day-12): reverse proxy load balancer with multi-replica support](https://github.com/kekcoke/kendo/pull/14) · merged `6c008a4`

### Changed
- Add NGINX reverse proxy (`ops/nginx/nginx.conf`) with upstream load balancing for gateway and userservice
- Docker Compose: backend services migrated from `ports:` to `expose:` for multi-replica compatibility
- Add NGINX service (`nginx:alpine`) as single entry point on port 5000
- CI pipeline: split into single-replica and multi-replica phases with `--scale gateway=3 --scale userservice=3 --scale worker=3`
- Infrastructure tests: 5 `[Category=Infrastructure]` tests for routing, distribution, and replica kill resilience
- Ops runbook: deployment playbook, three-tier rollback, and failure scenarios

### Deferred
- None — M3.1 delivered in full

### Fixed
- N/A — first Phase 03 milestone
