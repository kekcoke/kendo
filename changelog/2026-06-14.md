## Day 12 — M3.2: Stateless Validation & Redis Distributed Cache
**MR:** [feat(day-12): M3.2 Stateless validation & Redis distributed cache](https://github.com/kekcoke/kendo/pull/15) · merged `c303560`

### Changed
- Redis distributed cache (`AddStackExchangeRedisCache` / `AddDistributedMemoryCache` fallback) via `AddKendoDistributedCache()` in Kendo.Shared — wired into all 3 services (Gateway, UserService, Worker)
- ReplicaIdentityMiddleware — reads Docker `$HOSTNAME`, appends `X-Kendo-Replica` header to all HTTP responses
- NGINX passthrough: `proxy_set_header X-Kendo-Replica $upstream_http_x_kendo_replica;`
- Docker Compose: new Redis service (redis:7-alpine) with health check; `Redis__ConnectionString` env var on all 3 services
- CI pipeline: Redis service container, health threshold updated to 6/12, replica header verification step
- 7 new caching tests: replica identity middleware (3) + distributed cache registration (4) — 76/76 unit tests passing

### Deferred
- M3.3 Chaos suite — next milestone
- M3.4 Rate limiting & shedding
- M3.5 Graceful shutdown
- M3.6 Ops runbooks

### Fixed
- None — first M3.2 delivery

---

# Day 12 — M3.1: Reverse Proxy Load Balancer
**MR:** [feat(day-12): reverse proxy load balancer with multi-replica support](https://github.com/kekcoke/kendo/pull/14) · merged `6c008a4`

### Changed
- Add NGINX reverse proxy (`ops/nginx/nginx.conf`) with upstream load balancing for gateway and userservice
- Docker Compose: backend services migrated from `ports:` to `expose:` for multi-replica compatibility
- Add NGINX service (`nginx:alpine`) as single entry point on port 5000
- CI pipeline: split into single-replica and multi-replica phases with `--scale gateway=3 --scale userservice=3 --scale worker=3`
- Infrastructure tests: 5 `[Category=Infrastructure]` tests for routing, distribution, and replica kill resilience
- Ops runbook: deployment playbook, three-tier rollback, and failure scenarios

### Deferred
- None — M3.1 delivered in full

### Fixed
- N/A — first Phase 03 milestone
