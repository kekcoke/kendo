## Day 11 — M2.6: Traceparent propagation across producer and consumer
**MR:** [feat(day-11): M2.6 traceparent propagation across producer and consumer (#12)](https://github.com/kekcoke/kendo/pull/12) · merged `17bbbd5`

### Changed
- **UserService:** Added `OutboxMessage.TraceContext` column (nullable text) — captures `Activity.Current?.Id` at outbox write time for end-to-end trace correlation
- **UserService:** `OutboxRepository.AddAsync()` stores ambient W3C traceparent string when an Activity exists; stores null when no ambient Activity (graceful fallback)
- **UserService:** `OutboxRelayService.ProcessBatchAsync()` builds optional headers dictionary from `TraceContext` — passes `traceparent` as Rebus message header when non-null; skips header when null/empty
- **Worker:** `UserCreatedEventHandler.StartTraceActivity()` static helper extracts `traceparent` from Rebus `MessageContext` headers and creates child `Activity` via `Activity.SetParentId()`; defensive try/catch for malformed headers falls back to fresh trace
- **EF Migration:** `AddTraceContext` — adds `TraceContext` column to `OutboxMessages` table; no backfill needed (null values handled gracefully)
- **Tests:** 17 new unit tests (6 OutboxRepository trace tests + 3 OutboxRelayService trace header tests + 4 UserCreatedEventHandler trace correlation tests + 4 existing handler robustness tests); 94/94 passing

### Deferred
- None — Phase 02 complete, all milestones M2.1–M2.6 delivered

### Fixed
- None

---

## Day 08 — M2.3: Background consumer with idempotency key enforcement
**MR:** [feat(worker): M2.3 Background consumer with idempotency enforcement](https://github.com/kekcoke/kendo/pull/9) · merged `2b6d04c`

### Changed
- Added `IdempotencyRecords` table via EF Core migration in Worker (tracks message processing status by `MessageId` PK)
- Implemented `UserCreatedEventHandler` — consumes `UserCreatedEvent` from Rebus ASB queue with transactional idempotency enforcement
- Worker now registers both `WorkerDbContext` and `AppDbContext` for handler DI resolution; connected to PostgreSQL for idempotency + user status updates
- Worker Dockerfile: restore Worker's `appsettings.json` after publish to fix port conflict with UserService reference
- Worker MVC: isolated controller discovery to Worker assembly only to prevent ambiguous route matches with UserService's `HealthController`
- `docker-compose.yml`: Worker now has `ConnectionStrings__DefaultConnection` + `depends_on: postgres`
- 7 new unit tests covering: first-time processing, duplicate suppression, crash recovery, unknown user scenario, failed retry, unique constraint enforcement

### Deferred
- None

### Fixed
- None
