## Day 13 — M3.3: Chaos Test Suite
**MR:** [feat(day-13): chaos test suite](https://github.com/kekcoke/kendo/pull/16) · merged `aab3411`

### Changed
- Added `ChaosTestFixture` + `ChaosTestBase` — shared Docker CLI wrapper, HTTP client factory, health-assertion utilities for chaos testing
- Added `DbDowntimeChaosTests` — PostgreSQL pause triggers circuit breaker 503 fallback, verifies no cascading failure to Gateway
- Added `ServiceCrashChaosTests` — kill one replica, verify NGINX re-routes to remaining replicas, all requests return 200
- Added `NetworkPartitionChaosTests` — end-to-end outbox + async processing verification, graceful skip when broker unavailable
- Added `scripts/chaos/` — bash orchestration suite: `test_db_downtime.sh`, `test_service_crash.sh`, `test_network_partition.sh`, `run_all.sh` with structured [PASS]/[FAIL]/[SKIP] output
- Added `ops/runbooks/day_13_runbook.md` — chaos test runbook with recovery playbook
- Added `chaos-test` CI job in `.github/workflows/ci.yml` — runs after docker-compose job, invokes `run_all.sh` on multi-replica stack, uploads structured results

### Deferred
- None

### Fixed
- None

---

## Day 14 — M3.4: Rate Limiting & Load Shedding
**MR:** [feat(day-14): M3.4 Rate limiting & load shedding](https://github.com/kekcoke/kendo/pull/17) · merged `7ebef5b`

### Changed
- **Shared:** New `src/Shared/RateLimiting/` module — `RateLimitingOptions`, `RateLimitingServiceCollectionExtensions` (registers `FixedWindowRateLimiter` + `ConcurrencyLimiter` singletons), `RateLimitingMiddleware` (fixed-window, 429 + Retry-After), `LoadSheddingMiddleware` (concurrency limiter, 503 RFC 7807), `RateLimitingApplicationBuilderExtensions` (`UseKendoRateLimiter()`, `UseKendoLoadShedding()`)
- **Gateway:** Wired `AddKendoRateLimiting()`, `UseKendoLoadShedding()`, `UseKendoRateLimiter()` into middleware pipeline; added `RateLimiting` config section to `appsettings.json`
- **Middleware order:** `LoadSheddingMiddleware` → `RateLimitingMiddleware` → `ProblemDetailsMiddleware` → Controllers
- **Health bypass:** Both middleware bypass `/health/live` and `/health/ready` to prevent health-check cascade failures
- **Tests:** 8 new unit tests (4 rate limiter + 4 load shedding) covering 429/503, health bypass, Retry-After header, RFC 7807 content type
- **Ops:** `ops/runbooks/day_14_runbook.md` — config reference, failure scenarios, rollback plan

### Deferred
- M3.5 — Graceful shutdown (SIGTERM handler + drain timeout)
- M3.6 — Ops runbooks for each failure scenario
- Redis-backed distributed rate limiting (single-instance fixed window in this milestone)

### Fixed
- None
